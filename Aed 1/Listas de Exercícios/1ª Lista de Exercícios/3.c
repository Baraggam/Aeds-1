#include <stdio.h>
#include <stdlib.h>

int main()
{
    float lado = 10,
          area = lado*lado;
    printf("A area de um quadrado de lado %4.2f mede: %4.2f", lado, area);
    return 0;
}
